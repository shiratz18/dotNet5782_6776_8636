<Window x:Class="PL.CustomerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"  ResizeMode="NoResize" WindowStyle="None"
        Title="Customers" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="59*"/>
            <RowDefinition Height="375*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" Margin="0,0,0,0">
            <ListView x:Name="CustomersListView" Grid.ColumnSpan="3">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="100"/>
                        <GridViewColumn Header="NAME" DisplayMemberBinding="{Binding Name}" Width="100"/>
                        <GridViewColumn Header="PHONE" DisplayMemberBinding="{Binding Phone}" Width="100"/>
                        <GridViewColumn Header="?" DisplayMemberBinding="{Binding DeliveredFromCustomer}" Width="100"/>
                        <GridViewColumn Header="?" DisplayMemberBinding="{Binding NotDeliveredFromCustomer}" Width="100"/>
                        <GridViewColumn Header="?" DisplayMemberBinding="{Binding DeliveredToCustomer}" Width="150"/>
                        <GridViewColumn Header="?" DisplayMemberBinding="{Binding NotDeliveredToCustomer}" Width="150"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="CUTOMERS" FontSize="25"  Grid.Column="0" Margin="24,10,0,10" HorizontalAlignment="Left" FontWeight="Bold"/>

            <Button x:Name="btnAddCustomer" ToolTip="Click to add a new drone"  Content="+" Background="LightGray" Foreground="Black" Opacity="0.5" FontSize="70" HorizontalAlignment="Left" Width="96" Click="btnAddCustomer_Click" Grid.Column="3" Margin="38,304,0,-339">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="60"/>
                    </Style>
                </Button.Resources>
            </Button>
            <ComboBox x:Name="name" Margin="10,11,0,9" Grid.Column="1" HorizontalAlignment="Left" Width="158"/>
        </Grid>
    </Grid>
</Window>
